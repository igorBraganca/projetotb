# Arquivos a serem ignorados pelo git. Caso haja algum arquivo especifico em
# algum subdiretorio, crie um .gitignore no proprio subdiretorio do arquivo.
.*
*.o
*~
*.cgi
*.so
*.pyc
build/*.cgi
build/*.pyc
build/*.a
build/*.so
projetotb/*
!.gitignore
